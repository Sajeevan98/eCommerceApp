server:
  port : 8081

spring:
  application:
    name: productcatalog

  datasource:
    url: jdbc:mysql://localhost:3306/product-catalog
    username: root
    password:

  jpa:
    hibernate:
      ddlAuto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

